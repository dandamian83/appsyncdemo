package com.example.appsyncdemo;


public class AppSyncClient {

    // Create an AppSync client in your Spring Boot application:
//    BasicAWSCredentials awsCredentials = new BasicAWSCredentials("your-access-key", "your-secret-key");
//    AWSAppSyncClient client = AWSAppSyncClient.builder()
//            .withRegion(Regions.YOUR_REGION)
//            .withCredentials(new AWSStaticCredentialsProvider(awsCredentials))
//            .build();

    //In your Spring Boot application, when you want to send a message to the frontend, use the AppSync client to send a GraphQL mutation:

//    String mutation = "mutation CreateMessage { createMessage(content: \"Hello, World!\") { id content createdAt } }";
//    StartExecutionRequest request = new StartExecutionRequest().withApiId("your-appsync-api-id").withQuery(mutation);
//    StartExecutionResult result = client.startExecution(request);
}
